# 项目功能实现清单

本文档根据当前代码结构整理项目已具备的主要功能，并标注实现状态。

| 模块 | 功能 | 说明 | 状态 |
| --- | --- | --- | --- |
| 实时数据接入 | Track 航迹数据读取 | 读取时间、经纬度、高度、速度、航向等字段。 | 已实现 |
| 实时数据接入 | SCDP 数据读取 | 读取数浓度、液态水含量、中值体积直径、有效粒子直径和粒径 bins。 | 已实现 |
| 实时数据接入 | ICFP 数据读取 | 读取数浓度、液态水含量、中值体积直径、有效粒子直径和 bins。 | 已实现 |
| 实时数据接入 | MWR 数据读取 | 读取机表温度、机表湿度、云底高度、积分水汽、积分液态水和 0-1000 m 垂直廓线。 | 已实现 |
| 实时数据接入 | 增量文件读取 | 按追加内容读取实时增长的数据文件，避免重复处理。 | 已实现 |
| 实时数据接入 | 业务数据路径配置 | 通过 `config.py` 指定 Track、SCDP、ICFP、MWR 数据源路径。 | 已实现 |
| 实时数据接入 | 模拟数据备用源 | 非业务模式下可从 `simulated_data/` 读取模拟数据作为备用数据源。 | 已实现 |
| 实时数据接入 | MWR 分组暂存与刷新 | 对 MWR 分组记录进行暂存、补齐和过期刷新。 | 已实现 |
| 多源数据对齐 | 航迹主时间轴 | 以 Track 航迹时间作为统一对齐时间轴。 | 已实现 |
| 多源数据对齐 | 统一融合帧 | 将 Track、SCDP、ICFP、MWR 融合为 `AlignedFrame`。 | 已实现 |
| 多源数据对齐 | SCDP 同时刻匹配 | 将 SCDP 与航迹同时间点进行匹配。 | 已实现 |
| 多源数据对齐 | ICFP 回看匹配 | 在配置的回看窗口内匹配最近有效 ICFP 记录。 | 已实现 |
| 多源数据对齐 | MWR 保持匹配 | 在配置的保持时间内沿用最近有效 MWR 记录。 | 已实现 |
| 多源数据对齐 | 数据状态标记 | 对正常、缺失、沿用、部分数据等状态进行标记。 | 已实现 |
| 多源数据对齐 | 历史帧回填 | 当较晚到达的 ICFP 或 MWR 数据补齐历史时间窗时，回填已生成融合帧。 | 已实现 |
| MWR 饱和区识别 | 饱和区分类 | 根据温度廓线和湿度廓线识别 0-1000 m 饱和区。 | 已实现 |
| MWR 饱和区识别 | 水汽压计算 | 计算液水饱和水汽压、冰面饱和水汽压和实际水汽压。 | 已实现 |
| MWR 饱和区识别 | 分类编码输出 | 输出 `-1`、`0`、`1`、`2`、`3` 五类高度层编码。 | 已实现 |
| MWR 饱和区识别 | 温度层高度插值 | 计算 0 ℃ 高度和 -5 ℃ 高度。 | 已实现 |
| MWR 饱和区识别 | 连续区间合并 | 合并连续同类高度层，输出饱和区高度段摘要。 | 已实现 |
| 后端 API | 前端页面服务 | `GET /` 返回前端页面。 | 已实现 |
| 后端 API | 运行状态接口 | `GET /api/status` 返回数据计数、最新时间、轮询间隔、文件状态等信息。 | 已实现 |
| 后端 API | 最新数据接口 | `GET /api/latest` 返回最新融合帧。 | 已实现 |
| 后端 API | 历史数据接口 | `GET /api/history?seconds=300` 返回指定时间范围内的历史融合帧。 | 已实现 |
| 后端 API | 地图配置接口 | `GET /api/map-config` 返回底图、雷达、卫星云图相关配置。 | 已实现 |
| 后端 API | Himawari 元数据接口 | `GET /api/himawari/latest` 获取并缓存 Himawari 最新卫星云图元数据。 | 已实现 |
| 后端 API | 重点点位接口 | `GET /api/important-points` 读取并校验重点点位和路线配置。 | 已实现 |
| 后端 API | 实时 WebSocket | `WS /ws/realtime` 推送实时融合帧。 | 已实现 |
| 后端 API | 静态资源挂载 | 挂载 `frontend/` 静态资源目录。 | 已实现 |
| 后端 API | 本地瓦片挂载 | 挂载 `map_tiles/` 本地地图瓦片目录。 | 已实现 |
| 前端可视化 | 实时指挥看板 | 提供 `BY Weather Flight Command` 前端页面。 | 已实现 |
| 前端可视化 | WebSocket 实时刷新 | 接收实时融合数据并刷新页面。 | 已实现 |
| 前端可视化 | 历史数据初始化 | 页面首次加载时拉取已有历史数据。 | 已实现 |
| 前端可视化 | 实时/回放模式 | 支持实时模式和回放模式切换。 | 已实现 |
| 前端可视化 | 历史窗口设置 | 支持设置历史显示窗口分钟数。 | 已实现 |
| 前端可视化 | 回放点间隔设置 | 支持设置回放点时间间隔。 | 已实现 |
| 前端可视化 | 回放控制 | 支持播放、暂停和滑块选帧。 | 已实现 |
| 前端可视化 | 地图点击选帧 | 支持点击地图航迹点跳转到对应时间帧。 | 已实现 |
| 前端可视化 | 状态栏展示 | 显示连接状态、模式、最新时间、日期和历史窗口限制。 | 已实现 |
| 地图展示 | Leaflet 地图 | 使用 Leaflet 展示航迹地图。 | 已实现 |
| 地图展示 | 在线 OSM 底图 | 支持 OpenStreetMap 在线底图。 | 已实现 |
| 地图展示 | 本地离线瓦片 | 支持本地离线地图瓦片。 | 已实现 |
| 地图展示 | 在线卫星底图 | 支持在线卫星影像底图。 | 已实现 |
| 地图展示 | 底图失败回退 | 本地瓦片加载失败时可回退到在线底图。 | 已实现 |
| 地图展示 | 航迹绘制 | 绘制飞机航迹线和轨迹点。 | 已实现 |
| 地图展示 | 地图缩放状态 | 显示当前地图缩放级别。 | 已实现 |
| 地图展示 | 交互暂停刷新 | 用户拖动、缩放、滚动时暂停自动地图刷新。 | 已实现 |
| 地图展示 | 地图小窗模式 | 支持地图小窗/迷你模式相关交互。 | 已实现 |
| 地图展示 | 重点点位加载 | 从 `reference/important_points.json` 加载重点点位。 | 已实现 |
| 地图展示 | 重点路径加载 | 从配置加载重点路径、端点和标签。 | 已实现 |
| 地图展示 | 重要路径加载 | 补充：以雷州云雷达为例，其为雷达类重要端点，需要展示其10、20、30、40km的探测半径。同时划分12个方位角 | 已实现 |
| 地图展示 | 测距 | 开关使用，在地图上进行测距，具有上一步撤点功能（快捷键：Ctrl+Z）。 | 已实现 |
| 地图展示 | 测距 | 补充功能说明：测距时鼠标移动时也应显示距离数值。 | 已实现 |
| 地图展示 | 锚点 | 开关使用，在地图上标点，具有回退功能（快捷键:Ctrl+Z）; 点与点之间虚线(默认灰色)，锚点大小可修改 | 已实现 |
| 地图展示 | 锚点 | 补充说明：具有弹出式表格窗口和 TXT 导出功能。 | 已实现 |
| 地图展示 | 区域限定 | 具有左、右、上、下限定边界的提示功能，支持人为输入单边或多边边界（度-分-秒/度）。 | 已实现 |
| 地图展示 | 空域显示改为勾选 | 空域/重要路径显示调整为勾选框控制，默认不直接渲染；勾选后加载显示固定空域、重要点位、重要路径和雷达探测范围辅助线。 | 已实现 |
| 锚点输出 | 锚点 | 补充功能说明： 锚点输出的TxT格式应符合用户需求（询问） | 已实现 |
| 雷达与卫星叠加 | RainViewer 雷达叠加 | 支持天气雷达图层叠加。 | 已实现 |
| 雷达与卫星叠加 | RainViewer 覆盖范围 | 支持雷达覆盖范围图层叠加。 | 已实现 |
| 雷达与卫星叠加 | 雷达透明度调节 | 支持调节雷达图层透明度。 | 已实现 |
| 雷达与卫星叠加 | 雷达定时刷新 | 按配置周期刷新 RainViewer 雷达数据。 | 已实现 |
| 雷达与卫星叠加 | Himawari 云图叠加 | 支持 Himawari-9 卫星云图图层。 | 已实现 |
| 雷达与卫星叠加 | Himawari 多区域数据 | 支持全圆盘和日本区域数据组合。 | 已实现 |
| 雷达与卫星叠加 | Himawari 产品选择 | 支持红外 B13、可见光 B03 等产品配置。 | 已实现 |
| 雷达与卫星叠加 | 图片格式探测 | 自动选择可用卫星图像格式。 | 已实现 |
| 雷达与卫星叠加 | 元数据缓存 | 缓存 Himawari 元数据，减少重复请求。 | 已实现 |
| 局部云雷达叠加 | 元数据读取 | 指定时间间隔，刷新读取配置目录下 PPICMA、RPICMA 数据，支持 PPI/RPI 共用 CMA 径向基数据解析。 | 已实现 |
| 局部云雷达叠加 | 数据叠加 | 已读取数据，以雷达站经纬度为中心按极坐标 Canvas 叠加至地图上，优先级大于 RainViewer。 | 已实现 |
| 局部云雷达叠加 | 数据叠加 | 本地云雷达的探测范围视界与“叠加本地云雷达”联动，非默认加载；仅具备本地云雷达权限的账号开启本地云雷达后才绘制探测范围圆周和方位角线。 | 已实现 |
| 局部云雷达叠加 | 数据切换 | 未来可能会新增其他本地云雷达显示，应修改下拉框选指定云雷达加载，该功能仅全量账号具有 | 未实现 |
| 局部云雷达叠加 | 雷达探测范围复用 | 将本地雷达 10、20、30、40 km 探测圆周和方位角线从普通重要路径中拆出，作为可复用的雷达探测范围实体；未来可按多个雷达站点分别生成探测范围。 | 未实现 |
| 局部云雷达叠加 | 本地雷达列表选择 | admin/full 模式下本地云雷达应通过列表选择显示；当前仅接入一个雷达时列表显示雷达地区名称（如：雷州），未来支持多个本地云雷达同步接入和显示。 | 未实现 |
| 图表展示 | SCDP 时间序列 | 展示数浓度、液态水含量、中值体积直径、有效粒子直径。 | 已实现 |
| 图表展示 | ICFP 时间序列 | 展示数浓度、液态水含量、中值体积直径、有效粒子直径。 | 已实现 |
| 图表展示 | SCDP bins 柱状图 | 展示 SCDP 粒径 bins。 | 已实现 |
| 图表展示 | ICFP bins 柱状图 | 展示 ICFP bins。 | 已实现 |
| 图表展示 | MWR 标量时间序列 | 展示机表温度、机表湿度、云底高度、积分水汽、积分液态水。 | 已实现 |
| 图表展示 | MWR 温度廓线 | 展示 MWR 温度垂直廓线。 | 已实现 |
| 图表展示 | MWR 湿度廓线 | 展示 MWR 湿度垂直廓线。 | 已实现 |
| 图表展示 | MWR 水汽密度廓线 | 展示 MWR 水汽密度垂直廓线。 | 已实现 |
| 图表展示 | MWR 液态水廓线 | 展示 MWR 液态水垂直廓线。 | 已实现 |
| 图表展示 | MWR 饱和区热力图 | 展示 MWR 饱和区分类随时间和高度变化。 | 已实现 |
| 图表展示 | bins 图表开关 | 支持开关 SCDP 和 ICFP bins 图表显示。 | 已实现 |
| 模拟与测试 | 实时模拟器 | `simulate_realtime.py` 将源数据按时间节奏追加写入 `simulated_data/`。 | 已实现 |
| 模拟与测试 | 四类数据模拟 | 支持 Track、SCDP、ICFP、MWR 四类数据模拟输出。 | 已实现 |
| 模拟与测试 | 模拟参数配置 | 支持配置模拟间隔、随机抖动和跳过起始秒数。 | 已实现 |
| 模拟与测试 | bootstrap 数据生成 | 可从内置 bootstrap 数据生成缺失的模拟源文件。 | 已实现 |
| 模拟与测试 | 冒烟测试 | `smoke_test.py` 提供基础冒烟测试入口。 | 已实现 |
| 启动与配置 | 统一启动入口 | `launcher.py` 可作为统一启动入口。 | 已实现 |
| 启动与配置 | 运行目录设置 | 启动时设置运行目录和 `BY_WEATHER_BASE_DIR`。 | 已实现 |
| 启动与配置 | 外部配置加载 | 启动时加载外部 `config.py`。 | 已实现 |
| 启动与配置 | 日志输出 | 将 stdout/stderr 同时写入控制台和 `logs/` 日志文件。 | 已实现 |
| 启动与配置 | 自动打开浏览器 | 支持启动后自动打开浏览器。 | 已实现 |
| 启动与配置 | 网络参数配置 | 支持配置 `HOST`、`PORT`、`AUTO_OPEN_BROWSER`。 | 已实现 |
| 启动与配置 | 业务参数配置 | 支持配置数据路径、轮询间隔、对齐延迟、历史时间、地图、雷达、Himawari 和 MWR 高度层参数。 | 已实现 |
| 权限控制 | 具备登录与权限分区 | 支持内设账号密码、全量账号与精简账号；后端 API/WebSocket 按角色过滤数据，精简账号使用接近 `backend_lite` 的大地图布局，图表和本地云雷达控件隐藏。 | 已实现 |
| 日期修改 | 修改查看数据日期和架次 | full/lite 账号均可在界面临时输入数据日期和架次，切换后清空当前页面与后端缓存并按新路径读取；不写配置、不改源码，重启后仍以 `config.py` 默认日期和架次启动。 | 已实现 |
| 日期修改 | 修改查看数据机型 | 当前航迹数据默认机型为 B11；full/lite 账号可在界面临时输入或选择 B12、B13 等机型航迹，切换后按对应机型路径读取数据，不写配置、不改源码，重启后仍以默认机型启动。 | 已实现 |

## 未来计划建设内容

| 模块 | 计划内容 | 说明 | 状态 |
| --- | --- | --- | --- |
| 数据读取架构 | 长时段数据读取与前端性能优化 | 当前轨迹数据在线读取超过 1 小时后，Web 页面可能出现卡顿，同步观测数据加载也较繁重；后续需要评估数据读取、缓存、增量推送、前端抽稀/分页/窗口化渲染等优化路径。该项属于未来架构优化计划，不作为当前立即维护修改内容。 | 规划中 |


| 模块 | 功能 | bug说明 | 状态 |
| 地图展示 | 锚点 | 在地图上锚点，关闭随后再打开锚点功能，再Crtl+Z回退，无反应。 | 已修复 |
| 地图展示 | 锚点 | 在地图上锚点，Crtl+Z回退功能应具有针对性，在关闭锚点时，回退对锚点无效；重启锚点功能，回退才可以有效。 | 已修复 |
| 地图展示 | 测距 | 在地图上标点，Crtl+Z回退功能应具有针对性，在关闭测距时，回退对锚点无效；重启测距功能，回退才可以有效。 | 已修复 |
| 权限控制 | 精简账号界面 | 精简账号仅隐藏图表，没有参考 `backend_lite` 将地图扩大填充时序图和右侧图表区域；本地云雷达控件显示策略与权限要求不一致，lite 账号不应看到本地云雷达、PPI、RPI 控件。 | 已修复 |
| 权限控制 | Lite 本地云雷达控件泄露 | Lite 账号下仍可能显式看到“叠加本地云雷达”、PPI、RPI 和云雷达透明度控件；这些控件已改为默认隐藏，仅在账号具备 `view_local_radar` 权限后显示。 | 已修复 |
| 权限控制 | Lite 响应式布局泄露 | 窗口缩小后，lite 模式右侧边的数据窗口会暴露出来；已增加 lite 布局兜底规则，任意窗口宽度下都不显示或露出右侧数据窗口。 | 已修复 |
| 前端可视化 | 在线模式点击历史轨迹点 | 在线读取模式下，鼠标点击轨迹上的历史点或点击命中附近历史点时，不再自动切换为历史回放模式；保持在线模式，仅定位或查看被点击的历史点。 | 已修复 |
| 实时数据接入 | Reader 硬编码业务路径 | `readers.py` 中不应写死 `G:/B11` 和四类业务文件命名模板；路径规则已迁移至 `config.py::build_data_source_paths()`，Reader 仅消费配置生成结果。 | 已修复 |
| 地图展示 | 重要路径/固定路径层级 | 重要路径、固定路径和雷达探测范围位于 `importantPathPane`/固定路径相关 pane，层级调整为本地云雷达下方、RainViewer 雷达上方，并绑定独立 renderer。 | 已修复 |
| 地图展示 | 地图图层目标层级 | 地图业务图层目标顺序从上到下为：锚点、测距、航迹/飞机、区域边界、本地云雷达、重要路径/固定路径、RainViewer 雷达、RainViewer 覆盖范围、Himawari。 | 已实现 |
| 前端可视化 | 历史/实时模式切换卡顿 | 点击历史模式后再点击实时模式，页面可能无响应；历史回放模式中飞机点位可能跳到起始点；已改为回放飞机点优先跟随选中详细帧，切回实时模式时重新恢复最新历史窗口与全轨迹 overview。 | 已修复 |
| 前端可视化 | 历史回放观测数据不同步 | 点击更早的历史回放点时，航迹 overview 可选中但详细观测数据仍受 60min 缓存限制，图表不跟随对应时刻；已取消历史回放详细帧 60min 截断，实时显示窗口仍单独限制。 | 已修复 |
| 前端可视化 | WebSocket keepalive 超时 | 历史回放详细数据全量加载可能阻塞前端主线程，导致 WebSocket ping/pong 超时并出现 `ConnectionClosedError ... keepalive ping timeout`；已改为首屏仅加载实时窗口，完整回放走轻量 overview，点击历史点再按需拉局部观测窗口，并放宽 uvicorn WebSocket ping timeout。 | 已修复 |
| 前端可视化 | 右侧图表布局调整 | 右侧传感器图表中 SCDP 粒径谱与 ICFP 粒径谱位置对调；MWR 综合态势中的单值量图保留，但图内标题文字“MWR 单值量”不再显示。 | 已实现 |
| 前端可视化 | 时间窗口控件调整 | 左侧时间窗口中“历史长度”保留默认 10min 并使用独立确认按钮；“回放点间隔”保留默认 10s 并使用独立确认按钮；移除原“应用窗口”总按钮。 | 已实现 |
| 前端可视化 | 历史模式详细窗口统一 | 在线/实时模式逻辑保持不变；历史模式进入、播放和点击历史航迹点时，详细观测时间轴统一加载当前选中时刻附近 20min，避免默认末端 1h 与点击后 10min 不一致。 | 已实现 |
| 前端可视化 | Admin 弹性图表区折叠 | admin/full 图表权限下，下方时序图区域支持上下折叠，右侧传感器图表区域支持左右折叠；折叠时保留科技化箭头按钮用于展开，地图区域自动扩展填充释放空间。 | 已实现 |
| 前端可视化 | 过冷水汽饱和区图表留白优化 | “过冷水汽饱和区识别”标题仅保留在面板左上角，ECharts 内部标题不再占用绘图区；legend 与绘图区间距由 `MWR_ZONE_VISUAL_MAP_TOP`、`MWR_ZONE_GRID_TOP`、`MWR_ZONE_GRID_BOTTOM` 控制。 | 已实现 |
